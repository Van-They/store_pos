enum Status { success, failed, warning }

enum AppState {updated}

enum Language { kh, en }

// Regular expression to check if the string contains any letter (a-z or A-Z)
final RegExp letterRegex = RegExp(r'[a-zA-Z]');
